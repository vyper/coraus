<%= simple_form_for(talker) do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :phone %>
  <%= f.input :forecast_at, as: :date_time_picker %>

  <div class="actions">
    <%= f.submit 'Solicitar agendamento' %>
  </div>
<% end %>
